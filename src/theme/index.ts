'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // Kustomisasi theme sesuai kebutuhan
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default theme; 